[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = ["echo 'Backend built successfully'"]

[start]
cmd = "cd backend && npm start"

[variables]
NODE_ENV = "production" 